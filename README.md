Steps:

1. Select an ML Model and find its accuracy scores. This is first done without any encryption and the objective is to compare this with the results when we use encryption.

2. Choose a homomorphic scheme to encrypt test data. 

3. Design a method to convert ML classifier to intermediate representation that can be easily coded to operate over encrypted data.

4. Run the ML Model on encrypted test data. Compare results with the previous one. 

